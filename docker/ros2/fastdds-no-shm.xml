<?xml version="1.0" encoding="UTF-8"?>
<!-- Fast DDS profile: UDP-only transport (no shared memory).
     SHM in containers leaves stale /dev/shm lock files after any node crash,
     which then spams "Failed init_port ... open_and_lock_file failed" on the
     next run. UDPv4 loopback is plenty for the sim stack. -->
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <transport_descriptors>
    <transport_descriptor>
      <transport_id>udp_only</transport_id>
      <type>UDPv4</type>
    </transport_descriptor>
  </transport_descriptors>
  <participant profile_name="udp_only_participant" is_default_profile="true">
    <rtps>
      <userTransports>
        <transport_id>udp_only</transport_id>
      </userTransports>
      <useBuiltinTransports>false</useBuiltinTransports>
    </rtps>
  </participant>
</profiles>
