# Fast DDS and Gazebo transport receive bursts during Survey/Collection.
# The Ubuntu default (212992 bytes) produced reproducible RcvbufErrors even
# though Ethernet throughput and gateway latency remained low.
net.core.rmem_default = 4194304
net.core.rmem_max = 4194304
